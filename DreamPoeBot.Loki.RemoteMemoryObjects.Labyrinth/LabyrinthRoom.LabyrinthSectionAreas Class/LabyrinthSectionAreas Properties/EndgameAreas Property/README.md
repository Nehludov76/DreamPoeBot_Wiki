# EndgameAreas Property 

**Вложено элементов:** 0  

| LabyrinthRoom LabyrinthSectionAreas EndgameAreas Property |
| --- |


# LabyrinthRoom LabyrinthSectionAreas EndgameAreas Property

[Missing <summary> documentation for "P:DreamPoeBot.Loki.RemoteMemoryObjects.Labyrinth.LabyrinthRoom.LabyrinthSectionAreas.EndgameAreas"]


```csharp
public List < WorldArea > EndgameAreas { get ; }
```


```csharp
public List < WorldArea > EndgameAreas { get ; }
```


```csharp
public : property List < WorldArea ^>^ EndgameAreas { List < WorldArea ^>^ get ();
}
```


```csharp
public : property List < WorldArea ^>^ EndgameAreas { List < WorldArea ^>^ get ();
}
```
