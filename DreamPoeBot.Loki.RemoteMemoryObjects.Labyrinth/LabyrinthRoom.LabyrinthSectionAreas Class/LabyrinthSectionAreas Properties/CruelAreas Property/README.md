# CruelAreas Property 

**Вложено элементов:** 0  

| LabyrinthRoom LabyrinthSectionAreas CruelAreas Property |
| --- |


# LabyrinthRoom LabyrinthSectionAreas CruelAreas Property

[Missing <summary> documentation for "P:DreamPoeBot.Loki.RemoteMemoryObjects.Labyrinth.LabyrinthRoom.LabyrinthSectionAreas.CruelAreas"]


```csharp
public List < WorldArea > CruelAreas { get ; }
```


```csharp
public List < WorldArea > CruelAreas { get ; }
```


```csharp
public : property List < WorldArea ^>^ CruelAreas { List < WorldArea ^>^ get ();
}
```


```csharp
public : property List < WorldArea ^>^ CruelAreas { List < WorldArea ^>^ get ();
}
```
