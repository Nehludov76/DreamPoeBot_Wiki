# MagicProperties Property 

**Вложено элементов:** 0  

| HarvestExtraxtor MagicProperties Property |
| --- |


# HarvestExtraxtor MagicProperties Property

Note: This API is now obsolete.


```csharp
[ ObsoleteAttribute ("HarvestExtractor Object dont have this property anymore, MagicProperties will always return a empty list.")] public List < string > MagicProperties { get ; }
```


```csharp
[ ObsoleteAttribute ("HarvestExtractor Object dont have this property anymore, MagicProperties will always return a empty list.")] public List < string > MagicProperties { get ; }
```


```csharp
public :
[ ObsoleteAttribute (L"HarvestExtractor Object dont have this property anymore, MagicProperties will always return a empty list.")] property List < String ^>^ MagicProperties { List < String ^>^ get ();
}
```


```csharp
public :
[ ObsoleteAttribute (L"HarvestExtractor Object dont have this property anymore, MagicProperties will always return a empty list.")] property List < String ^>^ MagicProperties { List < String ^>^ get ();
}
```
