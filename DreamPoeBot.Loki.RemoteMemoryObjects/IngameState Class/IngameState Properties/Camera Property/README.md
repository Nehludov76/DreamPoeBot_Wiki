# Camera Property 


| IngameState Camera Property |
| --- |


# IngameState Camera Property

[Missing <summary> documentation for "P:DreamPoeBot.Loki.RemoteMemoryObjects.IngameState.Camera"]


```csharp
public Camera Camera { get ; }
```


```csharp
public Camera Camera { get ; }
```


```csharp
public : property Camera ^ Camera { Camera ^ get ();
}
```


```csharp
public : property Camera ^ Camera { Camera ^ get ();
}
```
