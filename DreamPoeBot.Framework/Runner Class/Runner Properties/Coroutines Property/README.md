# Coroutines Property 

**Вложено элементов:** 0  

| Runner Coroutines Property |
| --- |


# Runner Coroutines Property

[Missing <summary> documentation for "P:DreamPoeBot.Framework.Runner.Coroutines"]


```csharp
public IEnumerable < Coroutine > Coroutines { get ; }
```


```csharp
public IEnumerable < Coroutine > Coroutines { get ; }
```


```csharp
public : property IEnumerable < Coroutine ^>^ Coroutines { IEnumerable < Coroutine ^>^ get ();
}
```


```csharp
public : property IEnumerable < Coroutine ^>^ Coroutines { IEnumerable < Coroutine ^>^ get ();
}
```
