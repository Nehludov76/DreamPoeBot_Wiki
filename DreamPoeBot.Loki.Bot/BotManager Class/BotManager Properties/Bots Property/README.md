# Bots Property 

**Вложено элементов:** 0  

| BotManager Bots Property |
| --- |


# BotManager Bots Property

[Missing <summary> documentation for "P:DreamPoeBot.Loki.Bot.BotManager.Bots"]


```csharp
public static IReadOnlyList < IBot > Bots { get ; }
```


```csharp
public static IReadOnlyList < IBot > Bots { get ; }
```


```csharp
public : static property IReadOnlyList < IBot ^>^ Bots { IReadOnlyList < IBot ^>^ get ();
}
```


```csharp
public : static property IReadOnlyList < IBot ^>^ Bots { IReadOnlyList < IBot ^>^ get ();
}
```
