# Exceptions Property 


| BotManager Exceptions Property |
| --- |


# BotManager Exceptions Property

[Missing <summary> documentation for "P:DreamPoeBot.Loki.Bot.BotManager.Exceptions"]


```csharp
public static IReadOnlyList < Tuple < Exception , DateTime >> Exceptions { get ; }
```


```csharp
public static IReadOnlyList < Tuple < Exception , DateTime >> Exceptions { get ; }
```


```csharp
public : static property IReadOnlyList < Tuple < Exception ^, DateTime >^>^ Exceptions { IReadOnlyList < Tuple < Exception ^, DateTime >^>^ get ();
}
```


```csharp
public : static property IReadOnlyList < Tuple < Exception ^, DateTime >^>^ Exceptions { IReadOnlyList < Tuple < Exception ^, DateTime >^>^ get ();
}
```
