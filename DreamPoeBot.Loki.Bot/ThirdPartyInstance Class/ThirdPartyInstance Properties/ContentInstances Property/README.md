# ContentInstances Property 

**Вложено элементов:** 0  

| ThirdPartyInstance ContentInstances Property |
| --- |


# ThirdPartyInstance ContentInstances Property

[Missing <summary> documentation for "P:DreamPoeBot.Loki.Bot.ThirdPartyInstance.ContentInstances"]


```csharp
public IReadOnlyCollection < IContent > ContentInstances { get ; }
```


```csharp
public IReadOnlyCollection < IContent > ContentInstances { get ; }
```


```csharp
public : property IReadOnlyCollection < IContent ^>^ ContentInstances { IReadOnlyCollection < IContent ^>^ get ();
}
```


```csharp
public : property IReadOnlyCollection < IContent ^>^ ContentInstances { IReadOnlyCollection < IContent ^>^ get ();
}
```
