# RoutineInstances Property 

**Вложено элементов:** 0  

| ThirdPartyInstance RoutineInstances Property |
| --- |


# ThirdPartyInstance RoutineInstances Property

[Missing <summary> documentation for "P:DreamPoeBot.Loki.Bot.ThirdPartyInstance.RoutineInstances"]


```csharp
public IReadOnlyCollection < IRoutine > RoutineInstances { get ; }
```


```csharp
public IReadOnlyCollection < IRoutine > RoutineInstances { get ; }
```


```csharp
public : property IReadOnlyCollection < IRoutine ^>^ RoutineInstances { IReadOnlyCollection < IRoutine ^>^ get ();
}
```


```csharp
public : property IReadOnlyCollection < IRoutine ^>^ RoutineInstances { IReadOnlyCollection < IRoutine ^>^ get ();
}
```
