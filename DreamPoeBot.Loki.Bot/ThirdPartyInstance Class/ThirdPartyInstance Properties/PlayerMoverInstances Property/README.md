# PlayerMoverInstances Property 

**Вложено элементов:** 0  

| ThirdPartyInstance PlayerMoverInstances Property |
| --- |


# ThirdPartyInstance PlayerMoverInstances Property

[Missing <summary> documentation for "P:DreamPoeBot.Loki.Bot.ThirdPartyInstance.PlayerMoverInstances"]


```csharp
public IReadOnlyCollection < IPlayerMover > PlayerMoverInstances { get ; }
```


```csharp
public IReadOnlyCollection < IPlayerMover > PlayerMoverInstances { get ; }
```


```csharp
public : property IReadOnlyCollection < IPlayerMover ^>^ PlayerMoverInstances { IReadOnlyCollection < IPlayerMover ^>^ get ();
}
```


```csharp
public : property IReadOnlyCollection < IPlayerMover ^>^ PlayerMoverInstances { IReadOnlyCollection < IPlayerMover ^>^ get ();
}
```
