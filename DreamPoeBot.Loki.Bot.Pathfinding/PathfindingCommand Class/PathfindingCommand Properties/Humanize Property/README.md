# Humanize Property 


| PathfindingCommand Humanize Property |
| --- |


# PathfindingCommand Humanize Property

Note: This API is now obsolete.

[Missing <summary> documentation for "P:DreamPoeBot.Loki.Bot.Pathfinding.PathfindingCommand.Humanize"]


```csharp
[ ObsoleteAttribute ("Humanize is Deprecated, it is here to keep compatibility with old code but it do nothing at all.")] public bool Humanize { get ; set ; }
```


```csharp
[ ObsoleteAttribute ("Humanize is Deprecated, it is here to keep compatibility with old code but it do nothing at all.")] public bool Humanize { get ; set ; }
```


```csharp
public :
[ ObsoleteAttribute (L"Humanize is Deprecated, it is here to keep compatibility with old code but it do nothing at all.")] property bool Humanize { bool get (); void set ( bool value );
}
```


```csharp
public :
[ ObsoleteAttribute (L"Humanize is Deprecated, it is here to keep compatibility with old code but it do nothing at all.")] property bool Humanize { bool get (); void set ( bool value );
}
```
