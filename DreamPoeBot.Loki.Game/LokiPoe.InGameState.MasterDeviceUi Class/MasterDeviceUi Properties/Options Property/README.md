# Options Property 

**Вложено элементов:** 0  

| LokiPoe InGameState MasterDeviceUi Options Property |
| --- |


# LokiPoe InGameState MasterDeviceUi Options Property

Note: This API is now obsolete.


```csharp
[ ObsoleteAttribute ("Pls use the ZanaMods property", false )] public static List < Tuple < string , string , string >> Options { get ; }
```


```csharp
[ ObsoleteAttribute ("Pls use the ZanaMods property", false )] public static List < Tuple < string , string , string >> Options { get ; }
```


```csharp
public :
[ ObsoleteAttribute (L"Pls use the ZanaMods property", false )] static property List < Tuple < String ^, String ^, String ^>^>^ Options { List < Tuple < String ^, String ^, String ^>^>^ get ();
}
```


```csharp
public :
[ ObsoleteAttribute (L"Pls use the ZanaMods property", false )] static property List < Tuple < String ^, String ^, String ^>^>^ Options { List < Tuple < String ^, String ^, String ^>^>^ get ();
}
```
