# CapturedMonsters Property 


| LokiPoe InstanceInfo Bestiary CapturedMonsters Property |
| --- |


# LokiPoe InstanceInfo Bestiary CapturedMonsters Property


```csharp
public static List < BestiaryCapturedMonster > CapturedMonsters { get ; }
```


```csharp
public static List < BestiaryCapturedMonster > CapturedMonsters { get ; }
```


```csharp
public : static property List < BestiaryCapturedMonster ^>^ CapturedMonsters { List < BestiaryCapturedMonster ^>^ get ();
}
```


```csharp
public : static property List < BestiaryCapturedMonster ^>^ CapturedMonsters { List < BestiaryCapturedMonster ^>^ get ();
}
```
