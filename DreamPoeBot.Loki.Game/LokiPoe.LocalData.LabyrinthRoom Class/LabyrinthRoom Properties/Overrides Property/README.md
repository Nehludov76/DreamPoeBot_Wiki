# Overrides Property 

**Вложено элементов:** 0  

| LokiPoe LocalData LabyrinthRoom Overrides Property |
| --- |


# LokiPoe LocalData LabyrinthRoom Overrides Property

[Missing <summary> documentation for "P:DreamPoeBot.Loki.Game.LokiPoe.LocalData.LabyrinthRoom.Overrides"]


```csharp
public List < LokiPoe LocalData LabyrinthOverride > Overrides { get ; }
```


```csharp
public List < LokiPoe LocalData LabyrinthOverride > Overrides { get ; }
```


```csharp
public : property List < LokiPoe LocalData LabyrinthOverride ^>^ Overrides { List < LokiPoe LocalData LabyrinthOverride ^>^ get ();
}
```


```csharp
public : property List < LokiPoe LocalData LabyrinthOverride ^>^ Overrides { List < LokiPoe LocalData LabyrinthOverride ^>^ get ();
}
```
